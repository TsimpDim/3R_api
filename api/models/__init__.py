
'''With the 'models' folder created, we have to initialize a python module (hence `__init__.py`)

What this enables us to do is create separate .py's for each model instead of having a large `models.py`
'''